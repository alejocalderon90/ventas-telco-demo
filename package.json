{
  "name": "ventas-telco-demo",
  "version": "1.0.0",
  "private": false,
  "description": "Demo TELCO: front Express + backend FastAPI con consultas sobre dataset ficticio.",
  "scripts": {
    "front": "node server.js",
    "api": ".\\.venv\\Scripts\\python.exe -m uvicorn py_server:app --host 127.0.0.1 --port 8001 --reload",
    "dev": "npm-run-all --parallel api front"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
